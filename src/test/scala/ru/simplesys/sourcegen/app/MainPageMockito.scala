package ru.simplesys.sourcegen.app

/*
import org.specs2.mock.Mockito
import com.simplesys.servlet.http.{HttpSession, HttpServletResponse, HttpServletRequest}


object MainPageMockito extends Mockito {
    def apply(str: String) = {
        val request = mock[HttpServletRequest]
        val response = mock[HttpServletResponse]
        val session = mock[HttpSession]

        request.Session returns Some(session)

        response.PrintAndFlush(str) returns Unit
    }
}*/
